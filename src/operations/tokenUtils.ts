import { encode } from 'gpt-token-utils';

export function getTokenCount(text: string): number {
    //const tokens = encode(text);
    //return tokens.length;

    console.log('Congratulations, your extension "sy14141241242142141241 active!');

    return 0;
}